# ApexClubVerse – Apache rules for XAMPP / ProFreeHost
Options -Indexes

# Block direct web access to sensitive folders
RewriteEngine On
RewriteRule ^(includes|database|scripts)/ - [F,L]

# Deny access to hidden / config-like files
<FilesMatch "^\.">
    Require all denied
</FilesMatch>
